<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Tendies Tracker</title>
  <link href="https://fonts.googleapis.com/css?family=Material+Icons|Material+Icons+Outlined|Material+Icons+Two+Tone|Material+Icons+Round|Material+Icons+Sharp" rel="stylesheet">
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel='stylesheet' href='/stylesheets/auth-style.css' />
  <link rel="icon" href="/images/favicon.ico" />
</head>
<body>
  <section class="welcome-page">
    <div class="welcome-box">
      <img src="./images/logo.png" alt="tendie tracker" class="welcome-logo">
      <div>
        <h1 class="welcome-title">Tendies Tracker</h1>
        <p class="welcome-msg">Portfolio tracking for 💎 handed investors</p>
      </div>
    </div>

    <div class="auth-box">
      <h2>Create an account 🤝🏿</h2>

      <form action="" method="POST" class="signup-form" novalidate>  
        <fieldset>
          <label for="username">Username</label>
          <input type="text" name="username" id="username" value="<%= (typeof user === "undefined") ? "" : user.username %>" />
          <span class="error-msg">
            <% if (typeof errors !== "undefined" && Object.keys(errors).includes("username")) { %>
              <%= errors["username"].msg %>
            <% } %>
          </span>
        </fieldset>

        <fieldset>
          <label for="email">Email</label>
          <input type="email" name="email" id="email" value="<%= (typeof user === "undefined") ? "" : user.email %>" />
          <span class="error-msg">
            <% if (typeof errors !== "undefined" && Object.keys(errors).includes("email")) { %>
              <%= errors["email"].msg %>
            <% } %>
          </span>
        </fieldset>

        <fieldset>
          <label for="password">Password</label>
          <input type="password" name="password" id="password" autocomplete="on" value="<%= (typeof user === "undefined") ? "" : user.password %>" />
          <span class="error-msg">
            <% if (typeof errors !== "undefined" && Object.keys(errors).includes("password")) { %>
              <%= errors["password"].msg %>
            <% } %>
          </span>
        </fieldset>

        <fieldset>
          <label for="passwordConfirm">Password Confirmation</label>
          <input type="password" name="passwordConfirm" id="passwordConfirm" autocomplete="on" />
          <span class="error-msg">
            <% if (typeof errors !== "undefined" && Object.keys(errors).includes("passwordConfirm")) { %>
              <%= errors["passwordConfirm"].msg %>
            <% } %>
          </span>
        </fieldset>

        <button class="btn btn-primary">Sign Up</button>
      </form>

      <p>Already have an account? <a href="/sign-in">Log in</a></p>
    </div>
  </section>
</body>
</html>